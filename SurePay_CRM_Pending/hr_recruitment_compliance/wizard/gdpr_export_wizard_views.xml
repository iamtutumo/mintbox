<?xml version="1.0" encoding="utf-8"?>
<odoo>
    
    <!-- GDPR Export Wizard -->
    <record id="view_gdpr_export_wizard_form" model="ir.ui.view">
        <field name="name">gdpr.export.wizard.form</field>
        <field name="model">gdpr.export.wizard</field>
        <field name="arch" type="xml">
            <form string="Export Applicant Data">
                <group invisible="state == 'done'">
                    <field name="applicant_id" readonly="1"/>
                    <field name="export_format"/>
                    <field name="state" invisible="1"/>
                </group>
                <group invisible="state != 'done'">
                    <field name="export_file" filename="export_filename"/>
                    <field name="export_filename" invisible="1"/>
                    <field name="state" invisible="1"/>
                </group>
                <footer>
                    <button name="action_export" string="Generate Export" type="object" class="btn-primary" invisible="state == 'done'"/>
                    <button string="Close" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

</odoo>
